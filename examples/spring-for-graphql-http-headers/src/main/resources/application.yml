spring:
  graphql:

    graphiql:
      enabled: true
      path: /graphiql
    http:
      path: /api/graphql

server:
  port: 8081

# Exposes /actuator/health so CI / Conductor can poll readiness before running e2e tests
management:
  endpoints:
    web:
      exposure:
        include: health